# Agent and Workshop Contract

The C2 fixture consumes the shared step, template, citation, checkpoint, and symbolic-mating contracts without duplicating them.

Future agents use shared JSON contracts, retry one validation failure, and return the matching authored fallback if invalid. The workshop requests server authorization before unlocking checkpoint-gated progress.
