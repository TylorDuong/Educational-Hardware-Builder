# CAD Asset Metadata Contract

Consumers import `CadAssetRecord` from `@educational-hardware-builder/schemas` and consume only
its `matingFeatures` identifiers. Mating decisions remain symbolic; only the later deterministic
solver may produce an `AssemblyTransform`.
