# Educational-Hardware-Builder

Local-first software for teaching DIY hardware through guided, grounded builds.

## Workspace

Install the monorepo dependencies with:

```powershell
pnpm install
```

The workspace contains `apps/*` and `packages/*`; shared infrastructure, ingestion,
and seed-asset areas live in `infra/`, `ingestion/`, and `assets/`.
