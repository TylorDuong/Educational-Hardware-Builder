# Single-Developer Authored-Build Runner

Use the repository skill with one developer name:

```text
$sprint-run NAME=Tylor
```

The active queue is `specs/005-authored-build/tasks.md`. Read `AGENTS.md`, the constitution, the authored-build spec and plan, and the current `.sprint/authored-build.cursor` before acting. Do not use or modify the completed A/B/C sprint status, owner plans, execution guides, or historical merge train.

Start with the first unchecked task, complete one task at a time, update its checkbox only after evidence passes, and advance `.sprint/authored-build.cursor`. Preserve the weather-station fixture, `DEMO_SAFE_MODE`, citations, server-side checkpoints, symbolic model output, and solver-owned transforms.
